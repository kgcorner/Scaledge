<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>com.kgcorner</groupId>
		<artifactId>scaledge</artifactId>
		<version>1.0-SNAPSHOT</version>
	</parent>
	<groupId>com.kgcorner</groupId>
	<artifactId>scaledge-content</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>scaledge-content</name>
	<description>Photo gallery</description>

	<properties>
		<java.version>11</java.version>
		<docker.image.name>scaledge-content</docker.image.name>
	</properties>

	<dependencies>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<dependency>
			<groupId>com.kgcorner</groupId>
			<artifactId>scaledge-data</artifactId>
			<version>1.0-SNAPSHOT</version>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
        <dependency>
            <groupId>com.kgcorner</groupId>
            <artifactId>scaledge-common</artifactId>
            <version>1.0-SNAPSHOT</version>
            <scope>compile</scope>
        </dependency>
    </dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<profile >
			<id>build</id>
			<activation>
				<property>
					<name>buildDocker</name>
					<value>true</value>
				</property>
			</activation>
			<build>
				<plugins>
					<plugin>
						<groupId>com.spotify</groupId>
						<artifactId>docker-maven-plugin</artifactId>
						<configuration>
							<entryPoint>["java","-jar", "/${project.build.finalName}.jar"]</entryPoint>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>
		<profile >
			<id>build-debug-docker</id>
			<activation>
				<property>
					<name>buildDebugDocker</name>
					<value>true</value>
				</property>
			</activation>
			<build>
				<plugins>
					<plugin>
						<groupId>com.spotify</groupId>
						<artifactId>docker-maven-plugin</artifactId>
						<configuration>
							<entryPoint>["java","-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:5007","-jar", "/${project.build.finalName}.jar"]</entryPoint>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

</project>
